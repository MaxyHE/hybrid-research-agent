# 发布候选状态（2026-09-06）

## 已完成

- 独立Python 3.14虚拟环境安装 `pip install -e .` 成功，`pip check` 无依赖冲突。
- Node 26下 `npm ci`、Vite生产构建成功。安装发现的DOMPurify、brace-expansion、nanoid兼容补丁已更新到锁文件，更新后npm报告0项已知漏洞。构建仍有非阻断的大bundle提示。
- 隔离数据目录、8767端口启动，浏览器真实完成注册、首页访问、仅文库来源切换；缺文库时提交显示正确提示。视觉检查首页排版正常。
- 同步查询翻译功能及公开指标，附60查询与两侧排名和无API重算脚本；指标重算与研发结果一致。
- 补全Harness许可与组件来源说明，保留上游作者。
- 添加真实历史报告节选，明确不是实时网页演示。
- 对发布工作区和独立仓库历史884个blob进行了常见密钥格式、私钥标记和已知个人机器路径检查；唯一格式命中位于上游错误脱敏函数的文档示例。未发现私钥标记或已知个人路径；这不是第三方安全认证。发布路径中无`codex`，提交保留用户署名，不添加AI协作者。

## 发布前最后一项实际演示

用可用模型服务与演示文库完成：提交问题→后台研究→报告显示→引用打开→历史回看。此次免费Qwen端点连接重置，没有改用付费API，未将UI可访问误记为研究完成。

网页目前使用托管研究控制流，本地Qwen专用30题策略不因选择本地模型自动启用。README已说明，不能在演示中混淆。

本仓库未设远端、未公开推送。发布准备不等于公开部署到互联网。
